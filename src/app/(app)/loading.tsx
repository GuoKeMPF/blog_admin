import React, { Fragment, type FC } from "react";

type LoadingProps = {

};

export const Loading: FC<LoadingProps> = ({ }) => {
	return <Fragment>
		Loading
	</Fragment>;
};
export default Loading

